services:
  postgres:
    image: postgres:latest
    container_name: pg
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: postgres
      PGDATA: /data/postgres
    volumes:
      - ./volumes/postgres-data:/data/postgres
    ports:
      - "5434:5432"
  broker:
    image: apache/kafka:latest
    hostname: broker
    container_name: kafka
    ports:
      - 9092:9092
    environment:
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: broker,controller
      KAFKA_LISTENERS: PLAINTEXT://localhost:9092,CONTROLLER://localhost:9093
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://localhost:9092
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@localhost:9093
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: 0
      KAFKA_NUM_PARTITIONS: 3
#    command:
#      - /bin/bash
#      - -c
#      - |
#        cd /opt/kafka/bin
#        # Ждём пока Kafka станет доступна
#        while ! ./kafka-broker-api-versions.sh --bootstrap-server localhost:9092; do
#          echo "Waiting for Kafka to be ready..."
#          sleep 3
#        done
#        # Создаём топики
#        ./kafka-topics.sh --create --bootstrap-server localhost:9092 --topic updates --partitions 1 --replication-factor 1
#        ./kafka-topics.sh --create --bootstrap-server localhost:9092 --topic updates1 --partitions 1 --replication-factor 1
#        ./kafka-topics.sh --create --bootstrap-server localhost:9092 --topic updates-v2 --partitions 1 --replication-factor 1
#        ./kafka-topics.sh --create --bootstrap-server localhost:9092 --topic out-update --partitions 1 --replication-factor 1